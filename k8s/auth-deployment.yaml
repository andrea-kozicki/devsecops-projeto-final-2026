apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: studyboard
  labels:
    app: auth-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: auth-service
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/agent-requests-cpu: "25m"
        vault.hashicorp.com/agent-limits-cpu: "100m"
        vault.hashicorp.com/agent-requests-mem: "32Mi"
        vault.hashicorp.com/agent-limits-mem: "64Mi"
        vault.hashicorp.com/role: "studyboard-auth"
        vault.hashicorp.com/agent-inject-secret-auth.env: "secret/data/studyboard/auth-service"
        vault.hashicorp.com/agent-inject-template-auth.env: |
          {{- with secret "secret/data/studyboard/auth-service" -}}
          export AUTH_DB_USER="{{ .Data.data.AUTH_DB_USER }}"
          export AUTH_DB_PASS="{{ .Data.data.AUTH_DB_PASS }}"
          {{- end }}
    spec:
      serviceAccountName: auth-service-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 33
        runAsGroup: 33
        fsGroup: 33
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: auth-service
          image: studyboard-auth:local
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c"]
          args:
            - . /vault/secrets/auth.env && apache2-foreground
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: APP_ENV
              value: production
            - name: AUTH_DB_HOST
              value: auth-db
            - name: AUTH_DB_PORT
              value: "3306"
            - name: AUTH_DB_NAME
              value: studyboard_auth
            - name: AUTH_TOKEN_TTL
              value: "3600"
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 40
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 96Mi
            limits:
              cpu: 250m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
